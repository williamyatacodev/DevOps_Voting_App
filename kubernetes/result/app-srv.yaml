apiVersion: v1
kind: Service
metadata:
  name: result
  labels:
    app: result-295devops
    service: app-295devops
spec:
  type: NodePort
  ports:
  - name: service-result-app
    port: 5001
    targetPort: http-api
  selector:
    app: result
    service: app
